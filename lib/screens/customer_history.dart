import 'package:flutter/material.dart';

class CustomerHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('YOUR HISTORY 0.0'),
    );
  }
}
